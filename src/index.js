import React from "react";
import ReactDOM from "react-dom";
import "./index.css";
import "./App.css";

import App from "./App.js";

// Render the App component and mount it to the root element in the HTML
ReactDOM.render(<App />, document.getElementById("root"));
